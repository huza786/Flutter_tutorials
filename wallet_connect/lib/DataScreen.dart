// TrustWalletDataScreen.dart

import 'package:flutter/material.dart';

class TrustWalletDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trust Wallet Data'),
      ),
      body: Center(
        child: Text('Trust Wallet Data Here'),
      ),
    );
  }
}
